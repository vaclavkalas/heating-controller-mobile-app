#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../eq3-bluetooth-thermostat/bin/thermostat.js" "$@"
  ret=$?
else 
  node  "$basedir/../eq3-bluetooth-thermostat/bin/thermostat.js" "$@"
  ret=$?
fi
exit $ret
